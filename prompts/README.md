# Prompts

Prompt contracts for optional LLM-assisted stages live here.

Prompts should state:

- stage name and version
- required input artifacts
- allowed output fields
- fields the LLM must not invent
- required uncertainty or review markers
- downstream contract affected by the output

LLM prompts are never a substitute for schemas or validators.

